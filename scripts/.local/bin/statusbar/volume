#!/bin/sh

# Prints the current volume or 婢 if muted.

[ $(pamixer --get-mute) = true ] && echo 婢 && exit

vol="$(pamixer --get-volume)"

if [ "$vol" -gt "70" ]; then
	icon=" "
elif [ "$vol" -lt "30" ]; then
	icon=" "
else
	icon="墳 "
fi

echo "$icon$vol%"
